export default interface StakingPool {
    hashPool: string,
    name: string,
    inputAsset: string,
    ouputAsset: string,
    startDate: Date,
    endDate: Date,
    inputSymbol: string,
    outputSymbol: string
}